name: "Sub workflow"
on:
  workflow_run:
    workflows: ["Main workflow"]
    types:
      - completed
jobs:
  main:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Dump paylowd
        run: |
          echo "Payload: ${{ toJson(github.event.workflow_run) }}"
